<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Queara Post</title>
</head>
<body>
            <h1>Quara Post</h1>

            <% for(post of posts){ %>
                <div class="post">
                    <h3 class="user">@<%= post.username %></h3>
                    <h3 class="content"><%= post.content %></h3>
                    <br>

                    <a href="http://localhost:8080/posts/<%=post.id%>">See In Details</a>
                    <a href="http://localhost:8080/posts/<%=post.id%>/edit">EDIT</a><br>
                    <form action="/posts/<%= post.id %>?_method=DELETE" method="post">
                        <button>Delete</button>
                    </form>
                    
                   

                </div>
            <% }%>


            <br><br>
            <a href="http://localhost:8080/posts/new">Create New Post</a>
</body>
</html>

