<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For the Edit</title>
</head>
<body>
    <h2>Edit chat <%= chat._id %></h2>
    <p>This Chat Wase Sent Form <%= chat.form %> to <%= chat.to %></p>
    <form action="/chats/<%= chat._id %>?_method=PUT" method="post">
        <textarea name="msg" cols="30" rows="10"><%= chat.msg %></textarea>
        <button>Edit</button>
    </form> 
</body>
</html>